"""Project Cogni — own-voice Khmer long-form video assembly pipeline.

File-based core: each stage is a plain function that reads and writes files on
disk, so any stage can be re-run alone. See PROGRESS.md for the build order.
"""
