# Core
python-dotenv>=1.0.0
pyyaml>=6.0

# LLM access (OpenRouter — OpenAI-compatible client + raw httpx)
openai>=1.0.0
httpx>=0.27.0

# Document conversion (PDF/epub/docx -> markdown)
markitdown[pdf,docx]>=0.0.1

# Web UI
gradio>=5.0.0

# Images (placeholder generation + still handling)
pillow>=10.0.0

# Audio processing (measure durations, mix background music)
pydub>=0.25.0

# Whisper — ONLY for optional caption timing
openai-whisper>=20231117

# Utilities
requests>=2.31.0
